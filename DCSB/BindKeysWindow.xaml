<Window x:Class="DCSB.BindKeysWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DCSB"
        mc:Ignorable="d"
        WindowStyle="None"
        SizeToContent="WidthAndHeight"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        BorderBrush="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}"
        BorderThickness="1">

    <StackPanel>
        <StackPanel Margin="5,0">
            <Label Padding="0,5,0,0">Press keys or mouse buttons you want to bind.</Label>
            <Label Padding="0,0,0,5">(mouse clicks on this window are ignored)</Label>
        </StackPanel>
        <StackPanel Orientation="Horizontal"
                    FlowDirection="RightToLeft">
            <Button Margin="5" Width="50" Command="{Binding CancelBindKeysCommand}">Cancel</Button>
            <Button Margin="5" Width="50" Command="{Binding ClearKeysCommand}">Clear</Button>
        </StackPanel>
    </StackPanel>
</Window>
